---
---

<form>
    <button class="fake-checkbox"></button>
    <input type="text" name="task" id="todo" placeholder="What needs to be done?"/>
</form>

<style>
    .fake-checkbox{
        width: 1.8rem;
        height: 1.8rem;
        border-radius: 100vmax;
        display: grid;
        place-items: center;
        background-color: var(--color-bkg);
        border: 2px solid var(--color-neutral-1);
        transition: box-shadow 200ms cubic-bezier(0.075, 0.82, 0.165, 1);
    }

    form{
        display: flex;
        gap: 10px;
    }
</style>

<script>
    const form = document.querySelector("#todo") as HTMLFormElement;

    form?.addEventListener("submit", (e) => {
        e.preventDefault();

        const formData = new FormData(form); ``
        const task = formData.get("task");
    })
</script>